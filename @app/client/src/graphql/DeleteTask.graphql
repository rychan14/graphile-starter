mutation DeleteTask($id: UUID!) {
  deleteTask(input: { id: $id }) {
    clientMutationId
    task {
      id
      createdAt
      description
      id
      status
      title
      updatedAt
    }
  }
}
